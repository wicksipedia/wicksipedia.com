---
import IconX from "@/assets/icons/IconX.svg";
import IconMoon from "@/assets/icons/IconMoon.svg";
import IconSearch from "@/assets/icons/IconSearch.svg";
import IconArchive from "@/assets/icons/IconArchive.svg";
import IconSunHigh from "@/assets/icons/IconSunHigh.svg";
import IconMenuDeep from "@/assets/icons/IconMenuDeep.svg";
import LinkButton from "./LinkButton.astro";
import { SITE } from "@/config";

const { pathname } = Astro.url;

// Remove trailing slash from current pathname if exists
const currentPath =
  pathname.endsWith("/") && pathname !== "/" ? pathname.slice(0, -1) : pathname;

const isActive = (path: string) => {
  const currentPathArray = currentPath.split("/").filter(p => p.trim());
  const pathArray = path.split("/").filter(p => p.trim());

  return currentPath === path || currentPathArray[0] === pathArray[0];
};
---

<a
  id="skip-to-content"
  href="#main-content"
  class="absolute start-16 -top-full z-50 bg-background px-3 py-2 text-accent backdrop-blur-lg transition-all focus:top-4"
>
  Skip to content
</a>

<header class="header-wrap sticky top-0 z-40">
  <div class="app-layout">
    <div
      id="top-nav-wrap"
      class:list={[
        "flex items-center justify-between",
        "py-4 sm:py-5",
      ]}
    >
      <!-- Site title — bold, memorable -->
      <a
        href="/"
        class="group relative flex items-baseline gap-1.5 whitespace-nowrap"
      >
        <span class="text-xl font-extrabold tracking-tight text-foreground sm:text-2xl">
          {SITE.title}
        </span>
        <span class="header-dot"></span>
      </a>

      <!-- Navigation -->
      <nav
        id="nav-menu"
        class="flex items-center"
      >
        <button
          id="menu-btn"
          class="focus-outline p-2 sm:hidden"
          aria-label="Open Menu"
          aria-expanded="false"
          aria-controls="menu-items"
        >
          <IconX id="close-icon" class="hidden" />
          <IconMenuDeep id="menu-icon" />
        </button>
        <ul
          id="menu-items"
          class:list={[
            "header-menu-mobile",
            "hidden",
            "sm:flex sm:items-center sm:gap-1",
          ]}
        >
          <li>
            <a
              href="/blog"
              class:list={[
                "header-nav-link",
                { "header-nav-active": isActive("/blog") },
              ]}
            >
              Blog
            </a>
          </li>
          <li>
            <a
              href="/tags"
              class:list={[
                "header-nav-link",
                { "header-nav-active": isActive("/tags") },
              ]}
            >
              Tags
            </a>
          </li>
          <li>
            <a
              href="/about"
              class:list={[
                "header-nav-link",
                { "header-nav-active": isActive("/about") },
              ]}
            >
              About
            </a>
          </li>
          {
            SITE.showArchives && (
              <li>
                <LinkButton
                  href="/archives"
                  class:list={[
                    "header-nav-link flex items-center justify-center",
                    {
                      "header-nav-active [&>svg]:stroke-accent": isActive("/archives"),
                    },
                  ]}
                  title="Archives"
                  aria-label="archives"
                >
                  <IconArchive class="hidden sm:inline-block" />
                  <span class="sm:sr-only">Archives</span>
                </LinkButton>
              </li>
            )
          }

          <!-- Utility buttons: search + theme -->
          <li class="header-util-divider hidden sm:block" aria-hidden="true"></li>
          <li class="flex items-center justify-center">
            <LinkButton
              href="/search"
              class:list={[
                "header-icon-btn",
                { "[&>svg]:stroke-accent": isActive("/search") },
              ]}
              title="Search"
              aria-label="search"
            >
              <IconSearch />
              <span class="sr-only">Search</span>
            </LinkButton>
          </li>
          {
            SITE.lightAndDarkMode && (
              <li class="flex items-center justify-center">
                <button
                  id="theme-btn"
                  class="header-icon-btn relative"
                  title="Toggles light & dark"
                  aria-label="auto"
                  aria-live="polite"
                >
                  <IconMoon class="absolute top-[50%] left-[50%] -translate-[50%] scale-100 rotate-0 transition-all dark:scale-0 dark:-rotate-90" />
                  <IconSunHigh class="absolute top-[50%] left-[50%] -translate-[50%] scale-0 rotate-90 transition-all dark:scale-100 dark:rotate-0" />
                </button>
              </li>
            )
          }
        </ul>
      </nav>
    </div>
  </div>
</header>

<script>
  function toggleNav() {
    const menuBtn = document.querySelector("#menu-btn");
    const menuItems = document.querySelector("#menu-items");
    const menuIcon = document.querySelector("#menu-icon");
    const closeIcon = document.querySelector("#close-icon");

    if (!menuBtn || !menuItems || !menuIcon || !closeIcon) return;

    menuBtn.addEventListener("click", () => {
      const openMenu = menuBtn.getAttribute("aria-expanded") === "true";

      menuBtn.setAttribute("aria-expanded", openMenu ? "false" : "true");
      menuBtn.setAttribute("aria-label", openMenu ? "Open Menu" : "Close Menu");

      menuItems.classList.toggle("hidden");
      menuIcon.classList.toggle("hidden");
      closeIcon.classList.toggle("hidden");
    });
  }

  toggleNav();

  // Runs on view transitions navigation
  document.addEventListener("astro:after-swap", toggleNav);
</script>
